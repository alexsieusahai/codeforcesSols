t = int(input())
lineList = []
colList = []
sList = []
for case in range(t):
	rawStr = input()
	rawStr = rawStr.split()
	lineList.append(int(rawStr[0]))
	colList.append(int(rawStr[1]))
	sList.append(int(rawStr[2]))	
# test cases stored now
