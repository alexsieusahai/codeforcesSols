#include <iostream>
#include <cmath>

using namespace std;

int main()  {
    int n;
    cin >> n;
    int occurances[int(pow(10,5))];
    int temp;
    for (int i = 0; i < n; ++i) {
        cin >> 
    }
    cout << maxPoints(numList,n,pointsList);
    return 0;
}
